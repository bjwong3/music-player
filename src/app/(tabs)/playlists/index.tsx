import { defaultStyles } from '@/styles'
import { View } from 'react-native'

const PlaylistsScreen = () => {
	return <View style={defaultStyles.container}>Playlists Screen</View>
}

export default PlaylistsScreen
