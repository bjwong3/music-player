import { defaultStyles } from '@/styles'
import { View } from 'react-native'

const ArtistsScreen = () => {
	return <View style={defaultStyles.container}>Artists Screen</View>
}

export default ArtistsScreen
